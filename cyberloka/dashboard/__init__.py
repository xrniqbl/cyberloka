"""Cyberloka web dashboard (Flask).

Reads JSON report bundles produced by the CLI (`--reports-dir`) and renders an
interactive dashboard with scan history, filters, charts, and side-by-side
comparison.

Install the optional dependency first:

    pip install "cyberloka[dashboard]"
"""
